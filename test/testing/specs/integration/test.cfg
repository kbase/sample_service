[sampleservicetest]

# path to a temporary directory to use for tests.
# TODO: this depends on the host environment!
# This current setting matches the container; previously tests were run directly
# on someone's host machine
test.temp.dir = /tmp

# true to keep the temporary directory contents after running tests. 
test.temp.dir.keep = false
