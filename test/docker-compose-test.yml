version: "3.8"

# This docker-compose is unit testing workflows
services:
  test:
    image: sampleservice:test
    build:
      context: ..
    volumes:
      - "${PWD}:/kb/module"
    entrypoint: [ './test/scripts/entrypoint.sh' ]
    command: [ 'noop' ]

  